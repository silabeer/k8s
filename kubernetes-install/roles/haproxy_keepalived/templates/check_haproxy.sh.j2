#!/bin/sh
# {{ ansible_managed }}
#
# Проверяется именно наличие процесса, а не ответ на порту: до kubeadm init
# backend-узлы недоступны, и любая проверка через сам балансировщик будет
# провальной — VIP не поднимется, и установка не начнётся.
/usr/bin/killall -0 haproxy 2>/dev/null
