---
- name: Prepare
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Bootstrap python
      ansible.builtin.raw: |
        set -e
        apt-get update -qq
        apt-get install -y -qq python3 psmisc
      changed_when: true
