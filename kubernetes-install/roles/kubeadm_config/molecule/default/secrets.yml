---
# ТЕСТОВЫЕ секреты для molecule. Не ansible-vault и не production —
# сценарий должен проходить без внешних систем.
#
# Ключ детерминированный (байты 0x00..0x1f), 32 байта в base64.
# Production-ключ генерируется как `head -c 32 /dev/urandom | base64`
# и хранится в OpenBao.
etcd-encryption-key: "AAECAwQFBgcICQoLDA0ODxAREhMUFRYXGBkaGxwdHh8="
