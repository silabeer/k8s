---
- name: Prepare
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Bootstrap python and apt tooling
      ansible.builtin.raw: |
        set -e
        apt-get update -qq
        apt-get install -y -qq python3 ca-certificates curl gnupg
      changed_when: true
