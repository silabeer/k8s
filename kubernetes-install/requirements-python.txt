# Зависимости для РАБОТЫ playbook, не для тестов.
#
# hvac нужен lookup-плагину community.hashi_vault, то есть при
# secret_backend: openbao. Раньше он был закреплён только в
# requirements-test.txt — то есть попадал в тестовое окружение, а на машине
# оператора его не оказывалось, и первый же прогон с OpenBao падал.
hvac==2.3.0
